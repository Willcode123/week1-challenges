/* const suma = (a, b) => a + b;

console.log(suma(2, 3));  */

/*  const cuadrado = (num) => {
    return num * num;
  };
  

  console.log(cuadrado(6)); */

  /* let coffeeIsGrinding = false;
  const pressGrindBeans = () => {
    if (coffeeIsGrinding) {
      console.log("Stopping the grind");
      coffeeIsGrinding = false;
    } else {
      console.log("Grinding is about to begin");
      coffeeIsGrinding = true;
    }
  }
  
  pressGrindBeans();
   */

  /* let isLightOn = false;

const toggleLight = () => {
  if (isLightOn) {
    console.log("Turning off the light");
    isLightOn = false;
  } else {
    console.log("Turning on the light");
    isLightOn = true;
  }
}

toggleLight(); */


// function =>
/*  let parado  = true;

const elegir = () => {
  if (parado) {
    console.log("sientate");
    parado = true;
  } else {
    console.log("parate");
    parado = false;
  }
} 

elegir(); */

// Suma de variables simples
/* let a, b, c;
a = 5;
b = 6;
c = a + b;
console.log(c) */

/*  const person = {
    firstName: "John",
    lastName: "Doe"
  };
  
  console.log(person.firstName);  */


/*   let coffeeIsGrinding = false;

  const pressGrindBeans = () => {
    if (coffeeIsGrinding) {
      console.log("Stopping the grind");
      coffeeIsGrinding = false;
    } else {
      console.log("Grinding is about to begin");
      coffeeIsGrinding = true;
    }
  }
  pressGrindBeans() */

/*   let cashWithdralwal = (amount,accnum) => {
   console.log(withdrawing ${amount} from account number${accnum}) 
  }

  cashWithdralwal(300, accnumber); */

 /*  let aad Up = (num1, num2) => {
    retun num1 + num2
  }
  addUp(2,3)
  console.log() */

/*   const multiplyByNineFifths = (celsius) => {
    return celsius * (9/5);
  };
  
  const getFahrenheit = (celsius) => {
    return multiplyByNineFifths(celsius) + 32;
  };
  
  console.log("The temperature is " + getFahrenheit(15) + "°F");
  // Output: The temperature is 59°F */

/*   let multiplyByNineFifths = (celsius) => {
    return celsius * (9/5);
  };
  
  let getFah = (celsius) => {
    return multiplyByNineFifths(celsius) + 32;
  };
  
  console.log("The temperature is " + getFah(15) + "°F");
  // Output: The temperature is 59°F */

/* let subtract32 = (fahrenheit) => {
  return fahrenheit - 32;
};

let multiplyByFiveNinths = (fahrenheit) => {
  return (5/9) * subtract32(fahrenheit);
};

let getCelsius = (fahrenheit) => {
  return multiplyByFiveNinths(fahrenheit);
};

console.log("The temperature is " + getCelsius(59) + "°C"); */


